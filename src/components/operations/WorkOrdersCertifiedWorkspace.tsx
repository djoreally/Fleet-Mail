import React,{useCallback,useEffect,useMemo,useState}from'react';
import{AlertCircle,Eye,Loader2,Pencil,Plus,RefreshCw,Trash2,X}from'lucide-react';
import{fleetFetch}from'../../lib/fleetApi';

type WorkOrder={id:string;number:string;status:string;priority:string;complaint:string|null;diagnosis?:string|null;customerName:string|null;unitNumber:string|null;year:number|null;make:string|null;model:string|null;vehicleId?:string|null;customerId?:string|null;scheduledAt?:string|null;odometer?:number|null;engineHours?:number|null;purchaseOrderNumber?:string|null;requestedServices?:string[]|null;customerNotes?:string|null;technicianNotes?:string|null;updatedAt:string};
type Refs={vehicles:any[];customers:any[]};
const label=(value:string)=>String(value||'').replaceAll('_',' ').replace(/\b\w/g,x=>x.toUpperCase());
async function request<T>(url:string,init?:RequestInit):Promise<T>{const response=await fleetFetch(url,{...init,headers:{'Content-Type':'application/json',...(init?.headers||{})}});const body=await response.json().catch(()=>({}));if(!response.ok)throw new Error(body.error||`Request failed (${response.status})`);return body as T;}
const localDate=(value?:string|null)=>{if(!value)return'';const d=new Date(value);if(Number.isNaN(d.getTime()))return'';const pad=(n:number)=>String(n).padStart(2,'0');return`${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())}T${pad(d.getHours())}:${pad(d.getMinutes())}`};
export function WorkOrdersCertifiedWorkspace(){
 const[items,setItems]=useState<WorkOrder[]>([]),[refs,setRefs]=useState<Refs>({vehicles:[],customers:[]}),[loading,setLoading]=useState(true),[error,setError]=useState(''),[query,setQuery]=useState(''),[open,setOpen]=useState(false),[editing,setEditing]=useState<WorkOrder|null>(null),[viewing,setViewing]=useState<WorkOrder|null>(null),[busy,setBusy]=useState(false);
 const load=useCallback(async()=>{setLoading(true);setError('');try{const[data,choices]=await Promise.all([request<{workOrders:WorkOrder[]}>('/api/operations/work-orders'),request<any>('/api/fleet-operations/references')]);setItems(data.workOrders||[]);setRefs({vehicles:choices.vehicles||[],customers:choices.customers||[]});}catch(e){setError(e instanceof Error?e.message:'Unable to load work orders');}finally{setLoading(false)}},[]);useEffect(()=>{void load()},[load]);
 const rows=useMemo(()=>{const q=query.toLowerCase();return items.filter(x=>`${x.number} ${x.unitNumber||''} ${x.customerName||''} ${x.complaint||''}`.toLowerCase().includes(q))},[items,query]);
 const save=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();setBusy(true);setError('');const f=new FormData(e.currentTarget);const body={vehicleId:String(f.get('vehicleId')||''),complaint:String(f.get('complaint')||''),requestedServices:String(f.get('requestedServices')||''),purchaseOrderNumber:String(f.get('purchaseOrderNumber')||''),odometer:String(f.get('odometer')||''),engineHours:String(f.get('engineHours')||''),scheduledAt:String(f.get('scheduledAt')||''),priority:String(f.get('priority')||'routine'),customerNotes:String(f.get('customerNotes')||''),technicianNotes:String(f.get('technicianNotes')||'')};try{await request(editing?`/api/operations/work-orders/${editing.id}`:'/api/operations/work-orders',{method:editing?'PATCH':'POST',body:JSON.stringify(body)});setOpen(false);setEditing(null);await load()}catch(err){setError(err instanceof Error?err.message:'Save failed')}finally{setBusy(false)}};
 const remove=async(item:WorkOrder)=>{if(!window.confirm(`Delete ${item.number}? Only work orders without protected execution/history can be deleted.`))return;setBusy(true);setError('');try{await request(`/api/operations/work-orders/${item.id}`,{method:'DELETE'});await load()}catch(e){setError(e instanceof Error?e.message:'Delete failed')}finally{setBusy(false)}};
 return <main className="flex-1 overflow-y-auto bg-slate-50 p-4 sm:p-5 lg:p-8"><div className="mx-auto max-w-[1440px]"><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-bold uppercase tracking-widest text-blue-600">Certified service operations</p><h2 className="mt-1 text-3xl font-bold text-slate-950">Work Orders</h2><p className="mt-2 text-sm text-slate-600">Management edits preserve the full work packet. Execution status is controlled by the technician/dispatch workflow.</p></div><div className="flex flex-wrap gap-2"><button onClick={()=>void load()} className="flex min-h-11 items-center gap-2 rounded-xl border bg-white px-4 text-sm font-bold"><RefreshCw className="h-4 w-4"/>Refresh</button><button onClick={()=>{setEditing(null);setOpen(true)}} className="flex min-h-11 items-center gap-2 rounded-xl bg-blue-600 px-4 text-sm font-bold text-white"><Plus className="h-4 w-4"/>Create work order</button></div></div>{error&&<div className="mt-5 flex gap-2 rounded-xl border border-red-200 bg-red-50 p-3 text-sm font-semibold text-red-700"><AlertCircle className="h-5 w-5 shrink-0"/>{error}</div>}<div className="mt-6"><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search work order, unit, account, concern…" className="h-11 w-full max-w-md rounded-xl border bg-white px-3 text-sm"/></div>{loading?<div className="p-16 text-center text-slate-500"><Loader2 className="mx-auto h-6 w-6 animate-spin"/></div>:rows.length===0?<div className="mt-6 rounded-2xl border border-dashed bg-white p-12 text-center text-sm text-slate-500">No work orders found.</div>:<><div className="mt-5 grid gap-3 md:hidden">{rows.map(item=><article key={item.id} className="rounded-2xl border bg-white p-4 shadow-sm"><div className="flex items-start justify-between gap-3"><div><p className="font-bold text-blue-700">{item.number}</p><p className="mt-1 text-sm font-semibold text-slate-900">{item.unitNumber?`Unit ${item.unitNumber}`:'No unit'} · {item.customerName||'Fleet account'}</p></div><span className="rounded-full bg-slate-100 px-2.5 py-1 text-[11px] font-bold">{label(item.status)}</span></div><p className="mt-3 text-sm text-slate-600">{item.complaint||'No concern recorded'}</p><div className="mt-4 grid grid-cols-3 gap-2"><button onClick={()=>setViewing(item)} className="min-h-11 rounded-xl border text-sm font-bold">View</button><button onClick={()=>{setEditing(item);setOpen(true)}} className="min-h-11 rounded-xl border text-sm font-bold">Edit</button><button onClick={()=>void remove(item)} className="min-h-11 rounded-xl border border-red-200 text-sm font-bold text-red-600">Delete</button></div></article>)}</div><section className="mt-5 hidden overflow-hidden rounded-2xl border bg-white shadow-sm md:block"><div className="overflow-x-auto"><table className="min-w-[900px] w-full text-left text-sm"><thead className="bg-slate-50 text-[10px] uppercase text-slate-500"><tr>{['Work order','Vehicle','Customer','Concern','Priority','Status','Actions'].map(x=><th key={x} className="px-5 py-3">{x}</th>)}</tr></thead><tbody className="divide-y">{rows.map(item=><tr key={item.id}><td className="px-5 py-4 font-bold text-blue-700">{item.number}</td><td className="px-5 py-4">{item.unitNumber?`Unit ${item.unitNumber}`:'—'}</td><td className="px-5 py-4">{item.customerName||'—'}</td><td className="max-w-xs truncate px-5 py-4">{item.complaint||'—'}</td><td className="px-5 py-4">{label(item.priority)}</td><td className="px-5 py-4"><span className="rounded-full bg-slate-100 px-3 py-1.5 text-xs font-bold">{label(item.status)}</span></td><td className="px-5 py-4"><div className="flex gap-3"><button aria-label="View work order" onClick={()=>setViewing(item)}><Eye className="h-4 w-4"/></button><button aria-label="Edit work order" onClick={()=>{setEditing(item);setOpen(true)}}><Pencil className="h-4 w-4"/></button><button aria-label="Delete work order" onClick={()=>void remove(item)} className="text-red-500"><Trash2 className="h-4 w-4"/></button></div></td></tr>)}</tbody></table></div></section></>}
 {viewing&&<div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/40 p-3 sm:p-4"><div className="max-h-[92vh] w-full max-w-2xl overflow-y-auto rounded-2xl bg-white p-5 sm:p-6"><div className="flex justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-wider text-blue-600">Work packet</p><h3 className="text-xl font-bold">{viewing.number}</h3></div><button className="min-h-11 min-w-11 rounded-xl border" onClick={()=>setViewing(null)}><X className="mx-auto h-5 w-5"/></button></div><dl className="mt-5 grid gap-4 sm:grid-cols-2">{[['Status',label(viewing.status)],['Vehicle',viewing.unitNumber?`Unit ${viewing.unitNumber} · ${[viewing.year,viewing.make,viewing.model].filter(Boolean).join(' ')}`:'—'],['Fleet account',viewing.customerName||'—'],['Priority',label(viewing.priority)],['Requested services',(viewing.requestedServices||[]).join(', ')||'—'],['PO / authorization',viewing.purchaseOrderNumber||'—'],['Mileage',viewing.odometer?.toLocaleString()||'—'],['Engine hours',viewing.engineHours?.toLocaleString()||'—'],['Scheduled',viewing.scheduledAt?new Date(viewing.scheduledAt).toLocaleString():'—'],['Concern',viewing.complaint||'—'],['Customer notes',viewing.customerNotes||'—'],['Technician notes',viewing.technicianNotes||'—']].map(([k,v])=><div key={String(k)} className="rounded-xl border p-3"><dt className="text-xs font-bold uppercase text-slate-500">{k}</dt><dd className="mt-1 text-sm text-slate-800">{v}</dd></div>)}</dl></div></div>}
 {open&&<div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/40 p-3 sm:p-4"><form onSubmit={save} className="max-h-[94vh] w-full max-w-xl overflow-y-auto rounded-2xl bg-white p-5 sm:p-6"><div className="flex justify-between gap-3"><div><h3 className="text-xl font-bold">{editing?'Edit work order':'Create work order'}</h3>{editing&&<p className="mt-1 text-xs text-slate-500">Status changes happen in Dispatch/Technician execution, not this form.</p>}</div><button type="button" className="min-h-11 min-w-11 rounded-xl border" onClick={()=>{setOpen(false);setEditing(null)}}><X className="mx-auto h-5 w-5"/></button></div><div className="mt-5 grid gap-4"><select required={!editing} disabled={Boolean(editing)} name="vehicleId" defaultValue={editing?.vehicleId||''} className="min-h-11 rounded-xl border p-3 disabled:bg-slate-100"><option value="">Select vehicle…</option>{refs.vehicles.map(v=><option key={v.id} value={v.id}>Unit {v.unit_number} · {v.year||''} {v.make||''} {v.model||''}</option>)}</select><textarea required name="complaint" defaultValue={editing?.complaint||''} placeholder="Concern or requested service" className="rounded-xl border p-3"/><input name="requestedServices" defaultValue={(editing?.requestedServices||[]).join(', ')} placeholder="Requested services (comma-separated)" className="min-h-11 rounded-xl border p-3"/><input name="purchaseOrderNumber" defaultValue={editing?.purchaseOrderNumber||''} placeholder="PO number / authorization" className="min-h-11 rounded-xl border p-3"/><div className="grid gap-4 sm:grid-cols-2"><input name="odometer" defaultValue={editing?.odometer??''} inputMode="numeric" placeholder="Current mileage" className="min-h-11 rounded-xl border p-3"/><input name="engineHours" defaultValue={editing?.engineHours??''} inputMode="numeric" placeholder="Engine hours" className="min-h-11 rounded-xl border p-3"/></div><input name="scheduledAt" type="datetime-local" defaultValue={localDate(editing?.scheduledAt)} className="min-h-11 rounded-xl border p-3"/><select name="priority" defaultValue={editing?.priority||'routine'} className="min-h-11 rounded-xl border p-3">{['routine','high','emergency'].map(x=><option key={x} value={x}>{label(x)}</option>)}</select><textarea name="customerNotes" defaultValue={editing?.customerNotes||''} placeholder="Customer / fleet instructions" className="rounded-xl border p-3"/><textarea name="technicianNotes" defaultValue={editing?.technicianNotes||''} placeholder="Technician notes" className="rounded-xl border p-3"/></div><button disabled={busy} className="mt-5 min-h-12 w-full rounded-xl bg-blue-600 py-3 font-bold text-white disabled:opacity-50">{busy?'Saving…':editing?'Save without changing lifecycle':'Create work order'}</button></form></div>}</div></main>;
}
export default WorkOrdersCertifiedWorkspace;
