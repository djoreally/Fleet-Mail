import { and, desc, eq, sql } from 'drizzle-orm';
import { randomUUID } from 'node:crypto';
import { getDb } from '../../db/index.js';
import { customers, maintenanceEvents, maintenanceSchedules, organizations, vehicles, workOrders } from '../../db/drizzleSchema.js';

export type DueState='overdue'|'due_soon'|'upcoming'|'unknown';
export const WORK_ORDER_STATUSES=['draft','scheduled','assigned','en_route','arrived','in_progress','review','authorization_pending','authorized','complete','completed','cancelled'] as const;
export function calculateDueState(nextDueAt:Date|null,nextDueMileage:number|null,mileage:number|null,now=new Date(),nextDueEngineHours:number|null=null,engineHours:number|null=null):DueState{const milesLeft=nextDueMileage==null||mileage==null?null:nextDueMileage-mileage;const hoursLeft=nextDueEngineHours==null||engineHours==null?null:nextDueEngineHours-engineHours;const daysLeft=nextDueAt==null?null:(nextDueAt.getTime()-now.getTime())/86_400_000;if(milesLeft==null&&hoursLeft==null&&daysLeft==null)return'unknown';if((milesLeft!=null&&milesLeft<=0)||(hoursLeft!=null&&hoursLeft<=0)||(daysLeft!=null&&daysLeft<=0))return'overdue';if((milesLeft!=null&&milesLeft<=500)||(hoursLeft!=null&&hoursLeft<=25)||(daysLeft!=null&&daysLeft<=30))return'due_soon';return'upcoming'}
function database(){const db=getDb();if(!db)throw new Error('DATABASE_URL is not configured');return db}
export async function resolveOperationsOrganization(requested?:string){const configured=process.env.DEFAULT_ORGANIZATION_ID?.trim();const organizationId=requested?.trim()||configured;const db=database();if(organizationId){const[match]=await db.select({id:organizations.id}).from(organizations).where(eq(organizations.id,organizationId)).limit(1);if(!match)throw new Error('Organization was not found');return match.id}const[first]=await db.select({id:organizations.id}).from(organizations).orderBy(organizations.createdAt).limit(1);if(!first)throw new Error('No Fleet OS organization has been provisioned');return first.id}
export async function listWorkOrders(organizationId:string){const db=database();return db.select({id:workOrders.id,number:workOrders.number,status:workOrders.status,priority:workOrders.priority,complaint:workOrders.complaint,diagnosis:workOrders.diagnosis,completedAt:workOrders.completedAt,scheduledAt:workOrders.scheduledAt,technicianId:workOrders.technicianId,odometer:workOrders.odometer,engineHours:workOrders.engineHours,purchaseOrderNumber:workOrders.purchaseOrderNumber,requestedServices:workOrders.requestedServices,customerNotes:workOrders.customerNotes,technicianNotes:workOrders.technicianNotes,laborMinutes:workOrders.laborMinutes,travelMinutes:workOrders.travelMinutes,locationId:workOrders.locationId,createdAt:workOrders.createdAt,updatedAt:workOrders.updatedAt,vehicleId:vehicles.id,unitNumber:vehicles.unitNumber,year:vehicles.year,make:vehicles.make,model:vehicles.model,customerId:customers.id,customerName:customers.name}).from(workOrders).leftJoin(vehicles,and(eq(vehicles.id,workOrders.vehicleId),eq(vehicles.organizationId,organizationId))).leftJoin(customers,and(eq(customers.id,workOrders.customerId),eq(customers.organizationId,organizationId))).where(eq(workOrders.organizationId,organizationId)).orderBy(desc(workOrders.updatedAt))}
const requestedServicesFrom=(value:unknown)=>value==null?[]:Array.isArray(value)?value.map(String).map(v=>v.trim()).filter(Boolean):String(value).split(/[,\n]/).map(v=>v.trim()).filter(Boolean);
const optionalNumber=(value:unknown)=>{if(value===''||value==null)return null;const n=Number(value);if(!Number.isFinite(n)||n<0)throw new Error('Numeric values must be zero or greater');return n};
const optionalDate=(value:unknown)=>{if(value===''||value==null)return null;const d=new Date(String(value));if(Number.isNaN(d.getTime()))throw new Error('Date value is invalid');return d};
export async function createWorkOrder(organizationId:string,input:Record<string,unknown>){const db=database();const vehicleId=String(input.vehicleId||'');if(!vehicleId)throw new Error('vehicleId is required');const[vehicle]=await db.select({id:vehicles.id,customerId:vehicles.customerId,locationId:vehicles.locationId}).from(vehicles).where(and(eq(vehicles.organizationId,organizationId),eq(vehicles.id,vehicleId))).limit(1);if(!vehicle)throw new Error('Vehicle was not found in this organization');const generatedNumber=`WO-${new Date().getUTCFullYear()}-${Date.now().toString().slice(-6)}`;const status=String(input.status||'draft');if(!WORK_ORDER_STATUSES.includes(status as typeof WORK_ORDER_STATUSES[number]))throw new Error('Invalid work order status');const scheduledAt=optionalDate(input.scheduledAt);const[created]=await db.insert(workOrders).values({id:randomUUID(),organizationId,vehicleId,customerId:vehicle.customerId,locationId:input.locationId?String(input.locationId):vehicle.locationId,number:String(input.number||generatedNumber),status,priority:String(input.priority||'routine'),complaint:input.complaint?String(input.complaint):null,requestedServices:requestedServicesFrom(input.requestedServices),purchaseOrderNumber:input.purchaseOrderNumber?String(input.purchaseOrderNumber):null,odometer:optionalNumber(input.odometer),engineHours:optionalNumber(input.engineHours),customerNotes:input.customerNotes?String(input.customerNotes):null,technicianNotes:input.technicianNotes?String(input.technicianNotes):null,scheduledAt}).returning();return created}
export async function updateWorkOrder(organizationId:string,id:string,input:Record<string,unknown>){const db=database();const status=input.status==null?undefined:String(input.status);if(status&&!WORK_ORDER_STATUSES.includes(status as typeof WORK_ORDER_STATUSES[number]))throw new Error('Invalid work order status');const requestedServices=input.requestedServices==null?undefined:requestedServicesFrom(input.requestedServices);const[updated]=await db.update(workOrders).set({...(status?{status}:{}),...(input.priority!=null?{priority:String(input.priority)}:{}),...(input.complaint!=null?{complaint:String(input.complaint)}:{}),...(input.diagnosis!=null?{diagnosis:String(input.diagnosis)}:{}),...(input.scheduledAt!==undefined?{scheduledAt:optionalDate(input.scheduledAt)}:{}),...(input.technicianId!==undefined?{technicianId:input.technicianId?String(input.technicianId):null}:{}),...(input.locationId!==undefined?{locationId:input.locationId?String(input.locationId):null}:{}),...(input.odometer!==undefined?{odometer:optionalNumber(input.odometer)}:{}),...(input.engineHours!==undefined?{engineHours:optionalNumber(input.engineHours)}:{}),...(input.purchaseOrderNumber!==undefined?{purchaseOrderNumber:input.purchaseOrderNumber?String(input.purchaseOrderNumber):null}:{}),...(requestedServices!==undefined?{requestedServices}:{}),...(input.customerNotes!==undefined?{customerNotes:input.customerNotes?String(input.customerNotes):null}:{}),...(input.technicianNotes!==undefined?{technicianNotes:input.technicianNotes?String(input.technicianNotes):null}:{}),...(input.laborMinutes!==undefined?{laborMinutes:optionalNumber(input.laborMinutes)??0}:{}),...(input.travelMinutes!==undefined?{travelMinutes:optionalNumber(input.travelMinutes)??0}:{}),...((status==='complete'||status==='completed')?{completedAt:new Date()}:{}),updatedAt:new Date()}).where(and(eq(workOrders.organizationId,organizationId),eq(workOrders.id,id))).returning();if(!updated)throw new Error('Work order was not found');return updated}
export async function deleteWorkOrder(organizationId:string,id:string){const deleted=await database().delete(workOrders).where(and(eq(workOrders.organizationId,organizationId),eq(workOrders.id,id))).returning();return deleted.length>0}
export async function listMaintenance(organizationId:string){const rows=await database().select({id:maintenanceSchedules.id,vehicleId:maintenanceSchedules.vehicleId,serviceCode:maintenanceSchedules.serviceCode,program:maintenanceSchedules.program,intervalMiles:maintenanceSchedules.intervalMiles,intervalDays:maintenanceSchedules.intervalDays,intervalEngineHours:maintenanceSchedules.intervalEngineHours,lastServiceMileage:maintenanceSchedules.lastServiceMileage,lastServiceEngineHours:maintenanceSchedules.lastServiceEngineHours,lastServiceAt:maintenanceSchedules.lastServiceAt,nextDueMileage:maintenanceSchedules.nextDueMileage,nextDueEngineHours:maintenanceSchedules.nextDueEngineHours,nextDueAt:maintenanceSchedules.nextDueAt,active:maintenanceSchedules.active,unitNumber:vehicles.unitNumber,mileage:vehicles.mileage,engineHours:vehicles.engineHours,year:vehicles.year,make:vehicles.make,model:vehicles.model}).from(maintenanceSchedules).innerJoin(vehicles,and(eq(vehicles.id,maintenanceSchedules.vehicleId),eq(vehicles.organizationId,organizationId))).where(eq(maintenanceSchedules.organizationId,organizationId)).orderBy(maintenanceSchedules.nextDueAt);return rows.map(row=>({...row,dueState:calculateDueState(row.nextDueAt,row.nextDueMileage,row.mileage,new Date(),row.nextDueEngineHours,row.engineHours)}))}
export async function createMaintenanceSchedule(organizationId:string,input:Record<string,unknown>){const db=database();const vehicleId=String(input.vehicleId||'');const serviceCode=String(input.serviceCode||'').trim();if(!vehicleId||!serviceCode)throw new Error('vehicleId and serviceCode are required');const[vehicle]=await db.select({id:vehicles.id,mileage:vehicles.mileage,engineHours:vehicles.engineHours}).from(vehicles).where(and(eq(vehicles.organizationId,organizationId),eq(vehicles.id,vehicleId))).limit(1);if(!vehicle)throw new Error('Vehicle was not found in this organization');const intervalMiles=optionalNumber(input.intervalMiles),intervalDays=optionalNumber(input.intervalDays),intervalEngineHours=optionalNumber(input.intervalEngineHours);if(!intervalMiles&&!intervalDays&&!intervalEngineHours)throw new Error('At least one maintenance interval is required');const[created]=await db.insert(maintenanceSchedules).values({id:randomUUID(),organizationId,vehicleId,serviceCode,program:input.program?String(input.program):null,intervalMiles,intervalDays,intervalEngineHours,nextDueMileage:input.nextDueMileage==null?(intervalMiles&&vehicle.mileage!=null?vehicle.mileage+intervalMiles:null):Number(input.nextDueMileage),nextDueEngineHours:input.nextDueEngineHours==null?(intervalEngineHours&&vehicle.engineHours!=null?vehicle.engineHours+intervalEngineHours:null):Number(input.nextDueEngineHours),nextDueAt:input.nextDueAt?new Date(String(input.nextDueAt)):(intervalDays?new Date(Date.now()+intervalDays*86_400_000):null)}).returning();return created}
export async function completeMaintenance(organizationId:string,scheduleId:string,input:Record<string,unknown>){const db=database();const[schedule]=await db.select().from(maintenanceSchedules).where(and(eq(maintenanceSchedules.organizationId,organizationId),eq(maintenanceSchedules.id,scheduleId))).limit(1);if(!schedule)throw new Error('Maintenance schedule was not found');const mileage=optionalNumber(input.mileage),engineHours=optionalNumber(input.engineHours),occurredAt=input.occurredAt?new Date(String(input.occurredAt)):new Date();const notes=[input.notes?String(input.notes):'',engineHours!=null?`Engine hours: ${engineHours}`:''].filter(Boolean).join(' · ')||null;const[event]=await db.insert(maintenanceEvents).values({id:randomUUID(),organizationId,scheduleId,vehicleId:schedule.vehicleId,workOrderId:input.workOrderId?String(input.workOrderId):null,eventType:'completed',mileage,occurredAt,notes}).returning();await db.update(maintenanceSchedules).set({lastServiceMileage:mileage??schedule.lastServiceMileage,lastServiceEngineHours:engineHours??schedule.lastServiceEngineHours,lastServiceAt:occurredAt,nextDueMileage:schedule.intervalMiles&&mileage!=null?mileage+schedule.intervalMiles:schedule.nextDueMileage,nextDueEngineHours:schedule.intervalEngineHours&&engineHours!=null?engineHours+schedule.intervalEngineHours:schedule.nextDueEngineHours,nextDueAt:schedule.intervalDays?new Date(occurredAt.getTime()+schedule.intervalDays*86_400_000):schedule.nextDueAt,updatedAt:new Date()}).where(and(eq(maintenanceSchedules.organizationId,organizationId),eq(maintenanceSchedules.id,scheduleId)));if(mileage!=null||engineHours!=null)await db.update(vehicles).set({...(mileage!=null?{mileage}:{}),...(engineHours!=null?{engineHours}:{}),updatedAt:new Date()}).where(and(eq(vehicles.organizationId,organizationId),eq(vehicles.id,schedule.vehicleId),...(mileage!=null?[sql`${vehicles.mileage} IS NULL OR ${vehicles.mileage} <= ${mileage}`]:[])));return event}
